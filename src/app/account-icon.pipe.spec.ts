import { AccountIconPipe } from './account-icon.pipe';

describe('AccountIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountIconPipe();
    expect(pipe).toBeTruthy();
  });
});
